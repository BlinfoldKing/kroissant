pub struct Classifier {
    data: Vec<Vec<f64>>,
    class: Vec<f64>
}

impl Classifier {
    fn new(data: Vec<Vec<f64>>, class: Vec<f64>) -> Classifier {
        Classifier {
            data: data,
            class: class
        }
    }

    fn calculate_distance() {
    
    }

    fn get_class() {

    }
}
